version: 2.1

parameters:
  setup-config-param:
    default: "Running spiegel pipeline"
    type: string
  yml-file:
    default: "spiegel.yml"
    type: string
  yml-path:
    default: "deployment"
    type: string

jobs:
  spiegel-processing:
    docker:
      - image: cimg/base:2021.04
    steps:
      - run: echo "Running spiegel Pipeline."
workflows:
  spiegel-workflow:
    jobs:
      - spiegel-processing
